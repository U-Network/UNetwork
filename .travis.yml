language: go

install:
  - grep 'import' -rl ./| grep ".go"| xargs sed -i 's#"UGCNetwork/#"github.com/UGCNetworkProject/UGCNetwork/#g'
  - go get github.com/Masterminds/glide
  - glide install

go:
  - 1.8.x
